'use strict';

describe('entity tests', function(){
   it('create user', function(done){
      done();
   })
   it('create room', function(done){
      done();
   })
   it('create message', function(done){
      done();
   })
   it('create file', function(done){
      done();
   })
   it('get index', function(done){
      done();
   })
   it('get user', function(done){
      done();
   })
   it('get message', function(done){
      done();
   })
   it('get file', function(done){
      done();
   })
})

describe('controller tests', function(){
   it('login with user', function(done){
      done();
   })
   it('login with non-existing user', function(done){
      done();
   })
   it('find rooms', function(done){
      done();
   })
   it('send message', function(done){
      done();
   })
   it('get new messages', function(done){
      done();
   })
   it('enter room with user', function(done){
      done();
   })
   it('send direct message to user', function(done){
      done();
   })
   it('search for message', function(done){
      done();
   })
})